export type SiteConfig = {
  name: string;
  description: string;
};

export const siteConfig: SiteConfig = {
  name: "Move Info Central",
  description: "Lead generator for local and long distance moves.",
};
